# Changelog

## v1.1.0（2026-08-14）

安全加固（为公网/前置代理场景打底）：

- 新增设备令牌 + Cookie 绑定：每次批准签发随机令牌（32 位十六进制），设备经一次性认领链接换取 `lg_token` Cookie（HttpOnly + SameSite=Lax），后续请求与 WebSocket 升级均需携带匹配 Cookie
- 令牌只绑定第一个认领的浏览器；其他浏览器（或 Cookie 丢失）看到「已绑定到其他浏览器」页，需撤销后重新批准；重新批准自动换新令牌，旧 Cookie 立即失效
- 旧版批准记录无令牌字段，首次访问时自动补发令牌并进入认领流程（平滑升级，无需清空状态）
- 新增每 IP 每分钟限流（默认 120 次，滑动窗口），超限返回 429 + Retry-After，覆盖 HTTP 与 WebSocket 升级
- 新增可配置监听地址 `LISTEN_HOST`（默认 `0.0.0.0`），可收紧为 `127.0.0.1` 供前置反向代理/隧道接入
- 新增环境变量覆盖：`LAN_GATE_PORT`、`LAN_GATE_HOST`
- 修复：代理监听地址与日志原先硬编码 `0.0.0.0`

## v1.0.0（2026-08-14）

首个发布版本，自包含单文件插件 `lan-gate.mjs`：

- 内网反向代理（0.0.0.0:3088 → 127.0.0.1:3080），进程内实现，含 Host/Origin 重写与 WebSocket 升级转发
- 首次访问本机批准门禁（等待/拒绝页面，移动端设计）
- 每台设备独立访问方式：自动 / 手机（紧凑排版）/ 电脑
- 设置界面内嵌管理面板（状态、启停、批准/拒绝、方式切换、撤销、复制地址），仅本机可见
- 手机端适配：紧凑字号与间距、设置面板全屏化、模型/上下文弹层防遮挡、`crypto.randomUUID` polyfill
- 管理接口仅放行本机（x-forwarded-for 校验）；撤销即断开已建立连接
- 批准记录持久化到 `$DSH_HOME/lan-gate-state.json`
